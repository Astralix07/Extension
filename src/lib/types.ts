export interface App {
  id: string;
  name: string;
  description: string;
  category: string;
  version: string;
  downloadUrl: string;
  websiteUrl?: string;
  imageUrl: string;
  imageHint: string;
}
